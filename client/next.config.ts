import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['svc-21f28bfb-morphvm-04tuiacg.http.cloud.morph.so']
};

export default nextConfig;
